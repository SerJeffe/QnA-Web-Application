server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stackover?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: wombat
    platform: mysql
    initialization-mode: always
  jpa:
    hibernate:
      dialect: org.hibernate.dialect.MySQL8Dialect
      ddl-auto: update